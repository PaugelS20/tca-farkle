import { IonButton } from '@ionic/react';

export const Play: React.FC = () => {  
    return (
        <>
        <h2>Play</h2>
        <p>some data collection stuff goes here</p>
        {/* <IonButton onClick={() => nav("/setup")} id='startGameButton' color="success">Finish Game</IonButton> */}
        </>
      )
};