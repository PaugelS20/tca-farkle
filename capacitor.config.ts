import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paugel.farkle',
  appName: 'tca-farkle',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
